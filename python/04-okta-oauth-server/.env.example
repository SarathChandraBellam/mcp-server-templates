# Okta Configuration
# Create an Authorization Server in Okta Admin → Security → API
OKTA_DOMAIN=your-org.okta.com
OKTA_AUDIENCE=https://mcp-incidents-api

# Authorization Server ID — use "default" for Okta's built-in server,
# or a custom server ID (e.g. "aus1234567890abcdef")
OKTA_AUTH_SERVER_ID=default
